API 	to search repositories is /repositories
It accepts url parameter owner to filter on

All the repository hosting providers (Github, Gitlab etc.) can be configured in config/providerconfig.yml with corresponding urls, authorization and json key values
mapping so that repos 